---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: myminseok/java8_git_mvn
    tag: v1

inputs:
- name: source-repo
- name: build-out-repo

outputs:
- name: out-repo

run:
  path: /bin/bash
  args: 
    - build-out-repo/ci/tasks/build-test
