resources:
- name: ci-repo
  type: git
  source:
    branch: test 
    uri: git@github.com:myminseok/articulate-ci-demo.git
    private_key: {{github-private-key}}
  check_every: 10s # for demo

- name: source-repo
  type: git
  source:
    branch: master
    uri: git@github.com:myminseok/articulate.git
    private_key: {{github-private-key}}
  check_every: 10s # for demo


jobs:
- name: job1
  serial: true
  public: true
  plan:
  - get: ci-repo
  - get: source-repo
    trigger: true
  - task: task1
    file: ci-repo/ci/tasks/cachetest-task1.yml 

